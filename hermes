#!/usr/bin/env python3
"""
EVA Agent CLI launcher (compatibility alias for `eva`).

This wrapper should behave like the installed `eva` / `hermes` command,
including subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hermes_cli.main import main
    main()
